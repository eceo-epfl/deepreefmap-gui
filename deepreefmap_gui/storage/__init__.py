"""What the survey has put on each drive, and what of it can safely go.

``tiers``, ``inventory`` and ``reclaim`` are Qt-free and headless-testable;
``page`` and ``rows`` put them on screen behind the drive buttons at the foot of
the window.
"""
