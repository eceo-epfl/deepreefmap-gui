"""Shared UI primitives: theme tokens, fonts, painted icons, small widgets, the window protocol.

Nothing here knows about a feature, so anything may import it; the only outward references are the
TYPE_CHECKING ones in window_protocol.py.
"""
