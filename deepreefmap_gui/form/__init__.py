"""The run settings form: the widgets every run parameter is read from.

The form is never on screen, it is built hidden and lent to the settings dialog, so a setting with
no widget here has no way of reaching a run.
"""
