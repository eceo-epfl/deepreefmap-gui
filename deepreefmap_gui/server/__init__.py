"""The Server section: one registry, this device's credential, and the sync it drives.

`state.py` and `enrolment.py` are Qt-free reads and actions over `sync/`; the two
`_ui` modules are the page in the shell and the connect dialog.
"""
