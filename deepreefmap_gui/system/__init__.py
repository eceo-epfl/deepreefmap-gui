"""The machine's live state: the log console, and the RAM/VRAM/CPU/disk gauges.

Readings come from profiling/system_probe.py, the same source the pre-run guard decides on, so
what the user sees and what blocks a run can never disagree.
"""
