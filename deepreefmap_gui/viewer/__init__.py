"""The 3D point cloud viewer: the VTK widget, its overlays, and the window controls for them.

The widget implements the library's viewer protocol, so the orchestrator drives it stage by stage
while a run is still executing.
"""
