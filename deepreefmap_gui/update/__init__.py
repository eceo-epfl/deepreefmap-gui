"""The updater's UI: the Updates controls and the progress dialog that runs an install.

The work it drives (asset resolution, download, binary swap, environment provisioning) is Qt-free
and lives in packaging/.
"""
